

const INITIAL_STATE = {
    name: '',
    email: '',
    age: '',
};

function addClient(state = INITIAL_STATE, action) {
    switch(action.type) {
        default:
            return state;
    }
}

export default addClient;